.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Whether you use PnP or not, the node_modules folder is often used to store
# build artifacts that should be gitignored
node_modules

# Swap the comments on the following lines if you wish to use zero-installs
# In that case, don't forget to run `yarn config set enableGlobalCache false`!
# Documentation here: https://yarnpkg.com/features/caching#zero-installs

#!.yarn/cache
.pnp.*

# Circom compilation artifacts
*.r1cs
*.sym
*_js/
*.wtns


# Temporary circom-pairing clone (circuits are copied to groth16_verifier/lib/)
temp_circom_pairing/

# Groth16 verifier build artifacts
groth16_verifier/build/

# Generated witness files
generate_witness.js
witness_calculator.js

*.sym
